import styled from "styled-components/macro"

type Props = {

}
const FilterBar = () => {
    const Wrapper = styled.div`
    padding: 1rem;
    `

    return(
        <Wrapper>
            
        </Wrapper>
    )
}

export default FilterBar